extends layout

block content
  .row
    .col-md-4.col-md-offset-4
        .login-panel.panel.panel-default
            .panel-heading
                h3.panel-title Seja bem vindo
            .panel-body
                form(role='form')
                    fieldset
                        .form-group
                            input.form-control(placeholder='E-mail', name='email', type='email')
                        .form-group
                            input.form-control(placeholder='Password', name='password', type='password')
                        a.btn.btn-lg.btn-success.btn-block Login
    block script
        script.
            $(".btn").click(function(){
                window.location.href="/teste"
            })

    